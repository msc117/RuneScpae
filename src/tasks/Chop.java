package tasks;

import org.powerbot.script.wrappers.GameObject;

public class Chop {
/*	public void Chop() {
		if (ctx.backpack.count() == 28) {
			System.out.println("The inventory is full");
			//eventually add call to function to go to bank
			return;
		}
			
		if (ctx.objects.select().id(treeId).isEmpty()) { //no trees around
			System.out.println("There are no treas nearby");
			return;
		}
		GameObject tree = ctx.objects.nearest().poll();
		while (!tree.isOnScreen()) {
			ctx.camera.turnTo(tree);
			ctx.movement.stepTowards(tree);
			sleep(1000);
		}
		tree.interact("Chop");
	} */
}
